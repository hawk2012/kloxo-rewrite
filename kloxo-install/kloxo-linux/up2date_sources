### This describes the various package repositories (repos) that apt will
### query for packages. It currently supports APT repos.
### Format is one repository (repo) entry per line, # starts comments.
### The first word on each line is the type of repo.

### APT style repo. The format is:
### deb [options] url distribution component
deb [trusted=yes] https://raw.githubusercontent.com/hawk2012/kloxo-rewrite/main/ubuntu/ubuntu  main
deb [trusted=yes] https://raw.githubusercontent.com/hawk2012/kloxo-rewrite/main/debian/debian  main

### Testing repository (disabled by default)
### Do not use/enable this on production servers!
# deb [trusted=yes] https://raw.githubusercontent.com/hawk2012/kloxo-rewrite/main/testing/ubuntu/ubuntu  main
# deb [trusted=yes] https://raw.githubusercontent.com/hawk2012/kloxo-rewrite/main/testing/debian/debian  main

### Note: For APT repos, there can be multiple distributions and components specified.